import { Shield } from "lucide-react";
import { PlaceholderPage } from "./PlaceholderPage";

export function AdminPage() {
  return (
    <PlaceholderPage
      title="Admin"
      icon={Shield}
      description="Manage facility settings, staff accounts, permissions, and system configuration."
    />
  );
}
