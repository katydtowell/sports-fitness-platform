import { Clock } from "lucide-react";
import { PlaceholderPage } from "./PlaceholderPage";

export function TimeClockPage() {
  return (
    <PlaceholderPage
      title="Time Clock"
      icon={Clock}
      description="Track staff hours, manage time-off requests, and review timesheets."
    />
  );
}
