import { FileText } from "lucide-react";
import { PlaceholderPage } from "./PlaceholderPage";

export function DocumentsPage() {
  return (
    <PlaceholderPage
      title="Documents"
      icon={FileText}
      description="Manage waivers, contracts, forms, and other client documents."
    />
  );
}
