import { Calendar } from "lucide-react";
import { PlaceholderPage } from "./PlaceholderPage";

export function SchedulePage() {
  return (
    <PlaceholderPage
      title="Schedule"
      icon={Calendar}
      description="View and manage class schedules, appointments, instructor availability, and bookings."
    />
  );
}
