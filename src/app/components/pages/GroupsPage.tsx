import { UsersRound } from "lucide-react";
import { PlaceholderPage } from "./PlaceholderPage";

export function GroupsPage() {
  return (
    <PlaceholderPage
      title="Groups"
      icon={UsersRound}
      description="Create and manage client groups, rosters, and group memberships."
    />
  );
}
